// 12-8
// Remember that a Reducer can take 2 parameters, that is the current state
//   and the Action that being passed to that Reducer

export default (state, action) => {
    console.log(action);
    return null;
};

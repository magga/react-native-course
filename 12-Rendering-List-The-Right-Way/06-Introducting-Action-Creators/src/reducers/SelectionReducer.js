// 12-5
// We want to make another Reducer to save the current selected Library
// The current selected library then will be displayed as an expanded component
//   and we can see that library's description

// 12-5
// Here we set the default value of the SelectedReducer as a null value
// It means that when the app first boot, the selectedLibraryId will be null

export default () => {
    return null;
};
